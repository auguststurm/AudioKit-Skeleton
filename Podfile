# Uncomment the next line to define a global platform for your project
  platform :ios, '11.0'

target 'AudioKit-Skeleton' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'AudioKit', '~> 4.3'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Timepiece', '~> 1.3'

end
